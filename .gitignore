# Python
__pycache__/
*.py[cod]
*.pyo
.ipynb_checkpoints/

# Conda env — never commit this, recreate from environment_server.yml
pinn_env/

# Transient run directories (smoke tests, intermediate stage2)
runs/stage2/
runs/smoke/
runs/stage2_smoke/

# v2 methodology tree (see EXPERIMENTS.md) — mirror the v1 policy:
# keep walk_forward/ and stage2_* results (small); ignore smoke/transient.
runs/v2/smoke/
runs/v2/stage2/
runs/v2/stage2_smoke/

# Keep everything in runs/walk_forward/ (v1 + v2) and checkpoints/ (all small)

# macOS
.DS_Store
*.swp
